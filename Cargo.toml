[package]
name = "usb_audio_ctrl"
version = "0.2.0"
edition = "2021"

[dependencies]
rusb = "0.9"
tokio = { version = "1.0", features = ["full"] }
chrono = "0.4"
anyhow = "1.0"
alsa = "0.9"     # Direct ALSA interaction

[profile.release]
strip = true
lto = true
